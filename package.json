  {
  "name": "webmap",
  "version": "1.0.0",
  "description": "idea/concept of using a url list from spidering a target, formatted to json input, then changed to a mindMap style visual representation",
  "main": "init.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ryan Long",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.2",
    "ejs": "^2.5.6",
    "express": "^4.15.3",
    "socket.io": "^2.0.3",
    "vis": "^4.20.0"
  }
}
